package com.example.service;

import org.springframework.stereotype.Service;

@Service
public class MenuService {
    
    // 메뉴 관련 비즈니스 로직은 JavaScript에서 직접 API 호출로 처리
    // 서버 사이드에서는 더 이상 외부 API 호출을 하지 않음
    
}